git config --global credential.helper
git config --global credential.helper""
git config --global credential.helper ""
git config --global credentials.helper
git config --global credentials.helper""
git config --global credentials.helper ""
